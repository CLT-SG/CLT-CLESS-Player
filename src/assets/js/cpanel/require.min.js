var config
if (typeof(require) != "undefined") {
    var {
        ipcRenderer,
        desktopCapturer,
        remote
    } = require("electron")
    var win = remote.getCurrentWebContents()
    var path = require('path')
    var os = require('os')
    var homedir = os.homedir()
    config = require(homedir + '/clessapp/config')
}