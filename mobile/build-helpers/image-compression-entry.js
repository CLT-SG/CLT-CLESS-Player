/**
 * Entry point for bundling browser-image-compression library
 * This exports the imageCompression function as a global variable
 */
import imageCompression from 'browser-image-compression';

// Export as global for use in mobile app
export default imageCompression;
